def find_max_value(array)
 counter = 0
 while(array[counter]) do
  puts
 counter +=1
 end
end

#find the max value from the array
#assume the first # is 50 for example and compare the next elements to it 
# ex: if 51 is the next # then replace the 50 with the 51
#then 51 is the biggest # --> continue to compare until you find the biggest # in the array. 